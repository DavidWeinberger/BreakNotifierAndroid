/*
package at.htl.breaknotifierandroid

import androidx.test.InstrumentationRegistry
import androidx.test.runner.AndroidJUnit4
import at.htl.breaknotifierandroid.backend.BackendJava
import org.junit.Assert.assertEquals
import org.junit.Test
import org.junit.runner.RunWith

*/
/**
 * Instrumented test, which will execute on an Android device.
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 *//*

@RunWith(AndroidJUnit4::class)
class ExampleInstrumentedBackendJava {
    @Test
    fun useAppContext(){
        // Context of the app under test.
        val appContext = InstrumentationRegistry.getTargetContext()
        assertEquals("at.htl.breaknotifierandroid", appContext.packageName)
    }

    @Test
    fun getSchool() {
        val backend = BackendJava();
        val jObje = backend.getSchools("Htbla Leonding");
        assert(jObje.isEmpty());
    }
}
*/
